{
  "name": "@vfir/wasm",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build": "wasm-pack build --target web --release --out-dir pkg",
    "typecheck": "echo 'WASM package — no TS to check'"
  },
  "files": ["pkg/"]
}
